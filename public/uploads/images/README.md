# Cartella immagini

Inserisci qui le immagini (PNG, JPG, SVG, WebP) che vuoi mostrare nel portfolio.
Le path da usare nei JSON di `/content` sono relative a `/public`, ad esempio:

```
"image": "/uploads/images/avatar.png"
```
